# Reproducible Figures

This directory receives the outputs generated by the scripts in `src/`.

- **Figure 1:** trajectories of the canonical case for `u0 = 0.2, 0.8, 1.8, 2.0, 2.2, 3.0`.
- **Figure 2:** topological change around `kappa_c = 1.5625` for `chi=2.5`, `delta=1`.
- **Figure 3:** minimal structural-memory extension, comparing the same initial observable value `u0=1` with `m0=0` and `m0=0.7`.

PNG and PDF images are derived products. The primary reproducible scientific sources are the versioned equations, parameters, numerical checks, and scripts.
